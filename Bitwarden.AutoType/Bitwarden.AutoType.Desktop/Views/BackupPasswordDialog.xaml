<mah:MetroWindow x:Class="Bitwarden.AutoType.Desktop.Views.BackupPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        Title="Backup Vault" 
        Height="200" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowIconOnTitleBar="False"
        ShowMaxRestoreButton="False"
        ShowMinButton="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="Enter a password to encrypt your backup:" 
                   Margin="0,0,0,10"
                   FontSize="14"/>
        
        <PasswordBox Grid.Row="1" 
                     x:Name="PasswordInput"
                     mah:TextBoxHelper.Watermark="Backup password"
                     mah:TextBoxHelper.ClearTextButton="True"
                     Height="35"
                     FontSize="14"/>
        
        <TextBlock Grid.Row="2" 
                   Text="This password is used only for the backup file and is separate from your master password."
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   FontSize="11"
                   Margin="0,10,0,0"/>
        
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Cancel" 
                    Width="80" 
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button Content="Backup" 
                    Width="80" 
                    IsDefault="True"
                    Click="BackupButton_Click"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
